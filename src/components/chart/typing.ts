export interface SeriesDataItem {
  x: any;
  y: any;
}

export interface RadarDataItem {
  label: string;
  name: string;
  value: string | number;
}

export interface RadarIndicatorItem {
  name: string;
  max: number;
}
